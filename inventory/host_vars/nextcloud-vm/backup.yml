# =============================================================================
# nextcloud-vm - Backup Configuration
# =============================================================================

# Backup paths (B2 offsite)
restic_backup_paths_extra:
  - /opt
  - /home/johnny

# Local backup to ts440 ZFS (hourly)
local_restic_enabled: true
local_restic_backup_paths:
  - /opt
